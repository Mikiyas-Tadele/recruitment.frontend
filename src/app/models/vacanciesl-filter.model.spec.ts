import { VacancieslFilter } from './vacanciesl-filter.model';

describe('VacancieslFilter', () => {
  it('should create an instance', () => {
    expect(new VacancieslFilter()).toBeTruthy();
  });
});
